<div class="page-header">
  <%= link_to parent_path(@parent), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <h1>New kid</h1>
</div>

<%= render 'form' %>
