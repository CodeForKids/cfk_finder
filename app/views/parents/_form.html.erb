<%= form_for(@parent, :html => { :class => "form-horizontal", :role => "form" }) do |f| %>
  <% if @parent.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@parent.errors.count, "error") %> prohibited this parent from being saved:</h4>

      <ul>
      <% @parent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :first_name %>
    <%= f.text_field :first_name, :class => "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :last_name %>
    <%= f.text_field :last_name, :class => "form-control" %>
  </div>

  <%= f.fields_for :user do |u| %>
    <div class="form-group">
      <%= u.label :email %>
      <%= u.email_field :email, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= u.label :password %> <i>(<%= t('.leave_blank_if_you_don_t_want_to_change_it', :default => "leave blank if you don't want to change it") %>)</i><br />
      <%= u.password_field :password, :autocomplete => "off", class: "form-control" %>
    </div>
    <div class="form-group">
      <%= u.label :password_confirmation %><br />
      <%= u.password_field :password_confirmation, class: "form-control"  %>
    </div>
    <div class="form-group">
      <%= u.label :current_password %> <i>(<%= t('.we_need_your_current_password_to_confirm_your_changes', :default => 'we need your current password to confirm your changes') %>)</i>
      <%= u.password_field :current_password, class: "form-control" %>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
